projection;
strict ( 1 );
use draft;

define behavior for zcs_certif_prod alias Certificate
{
  use create;
  use update;
  use delete;

  use association _Status { create; with draft;}

  // Uso das ações de draft.
  use action Activate;
  use action Discard;
  use action Edit;
  use action Prepare;
  use action Resume;

}

define behavior for ZCS_CERTIF_PROD_ST alias CertificateSt
{
  use delete;

  use association _Certif;
}